# Batch 3 执行清单

版本：1.0（2026-09-14）
依据：`08_完整改进实施计划_v2.md` Batch 3 ＋ 拷问回合八项决策
范围：证据系统重做、发药对照三栏、时间线呈现、留言板内容与皮肤、存档升级、自动化替代盲测。

---

## 0. 拷问回合决策记录

| # | 问题 | 决策 |
|---|---|---|
| 1 | 本轮推进到哪里 | 只做 Batch 3，完成后第一、二章可盲测 |
| 2 | 前十分钟盲测门槛 | 自动化替代盲测（全路线测试＋四档截图＋前十分钟走查清单），线下盲测后置 |
| 3 | 文档 07 剧情正典地位 | 逐项确认；证据语言、任务结构、页面模板立即执行，剧情改动走提案审批 |
| 4 | 存档兼容 | 破坏性升级 schemaVersion 2→3，旧档提示重开，不写迁移 |
| 5 | 证据 27→18 收缩时机 | 本轮只建系统不剪；降级清单附于 `10_剧情变更提案.md`，Batch 4 评审统一剪 |
| 6 | 周砚死结（第一章错误解释 vs 逐项确认） | 提案入清单，批准后同轮补内容；其余 Batch 3 不依赖周砚 |
| 7 | 时间不够时的牺牲顺序 | 先牺牲留言板皮肤，其次时间线视觉强化；证据系统、发药三栏、完成反馈、测试不裁 |
| 8 | 交付物形式 | 本清单＋`10_剧情变更提案.md` 两份文档 |

---

## 1. 代码现状核查结论（2026-09-14）

| 项 | 现状 | 差距 |
|---|---|---|
| `InvestigationRail.vue` | 已有目标卡＋四入口＋最近 4 份证据速览 | 无完整证据抽屉、无固定托盘 `0/3`、无冲突计数 |
| `EvidenceCard.vue` | 标题优先、来源三档徽章（本机/原始/派生）、加入对照按钮 | 状态五档未统一；来源语言未映射为四档玩家语言；固定无上限 |
| `game.ts togglePin` | 直接 push，无数量限制 | 缺"最多固定三份"约束 |
| `MedicationPage.vue` | 功能完整：p1 冲突判定、助手捷径、AUD01、p2 时间线 | "谜题一/二"标题、EV 编号复选框（未取得时禁用）、"✓ EV12 已取得"反馈、单栏长列表——全部违反 07 册 §8.3 |
| `ForumPage.vue` | 梦境帖关联＋陈述采集可用 | 无周砚、无时间流皮肤、站务区露出 `EV14` 编号 |
| `evidence.json` | 27 份，`originGroup` 20 组、`sourceType` 三档 | 需映射四档玩家来源语言；EV28（周砚）待批新增 |
| `types.ts` | `schemaVersion: 2` | 升 3，加固定上限后的存档字段 |
| 测试 | reducer 6 套＋e2e 12 套（含六路线、降级、移动端） | 改动后需全量回归 |

---

## 2. 执行项

### A. 证据系统（P0，不可裁）

- [x] **A1 来源四档语言映射**：`selectors.ts` 新增玩家来源标签函数——归档链内（PRIMARY 且 originGroup 属院内链）、独立来源（论坛/地方档案/剧场类）、本机证据（PLAYER_LOCAL）、派生结论（DERIVED）。`EvidenceCard`、证据抽屉、材料页统一调用，`originGroup` 原码退为二级元数据。
- [x] **A2 状态五档统一**：未查看 / 已取得 / 已固定 / 存在冲突 / 已用于结论。冲突档由既有矛盾证据（EV11 等）派生；"已用于结论"由已通过谜题的证据集合派生。全部在 selectors 层计算，不改事件模型。
- [x] **A3 固定上限三份**：`togglePin` 加上限与满员提示；`InvestigationRail` 固定托盘显示 `已固定 n/3`；满员时第四份的固定按钮禁用并说明"对照托盘已满，先移出一份"。
- [x] **A4 证据抽屉升级**：`InvestigationRail` 的"已存证据"展开后显示完整列表（非最近 4 份），每行＝自然语言标题＋状态＋来源四档；未获得且当前可获得的条目显示来源页与"回到这里取得"动作，不显示禁用复选框。
- [x] **A5 编号退位**：所有玩家可见列表、按钮、反馈中，自然语言标题在前，`EVxx` 只保留在小号等宽元数据位（`EvidenceCard` 已基本达标，补齐 Medication/Forum/完成任务反馈）。

### B. 发药对照页三栏重做（P0，不可裁）

- [x] **B1 三栏结构**：`MedicationPage.vue` 重构为 材料箱（左，原始单据）/ 中央对照（谜题操作）/ 结论区（右，提交与反馈）；1020px 以下纵向堆叠为 任务—材料—提交。
- [x] **B2 自然语言化**："异常复查 · 谜题一"→"异常复查：少掉的是一条数据，还是一个人？"；"时间线重排 · 谜题二"→"按事件发生顺序重排当晚记录"；证据选项全部用标题（R03 护理事实与授权范围……），EV 编号进小号元数据；未取得的证据显示"回到材料箱取得"，不再是禁用复选框。
- [x] **B3 两阶段任务**：先完成"是否少了一个人"（p1＋身份恢复），时间线入口在此之前只显示为"下一步预告"，不与首屏争夺注意力（现有 tab 门控保留，改为任务卡式呈现）。
- [x] **B4 完成反馈面板**：p1 通过、身份恢复、p2 通过、AUD01 确认四处，按 08 册 §4.3 输出四段式——证明了什么／排除了哪个旧解释／开放了哪个入口／唯一继续按钮。删除"✓ EV12 已取得"式编号反馈。
- [x] **B5 即时教学**：第一次提交冲突时插入"独立来源"规则卡（六条须知拆散教学的对应项），一次性展示，可收起。
- [x] **B6 助手捷径保留**：行为与测试钩子（`p05:*` testid）不变，仅随三栏重排调整布局。

### C. 时间线呈现（P1，可简化不可裁）

- [x] **C1** 保留现有排序逻辑与判定（`checkP2` 不动），呈现改为带事件/上传双时间轴说明的任务卡；"标记 EV11 为不可靠"改为自然语言"《离院报告与模板清单》与其他记录矛盾，标记为不可靠"。
- [x] **C2** 提交成功后并入 B4 四段式反馈，"去病友留言板"成为唯一继续按钮。

### D. 留言板（P2，皮肤可牺牲）

- [x] **D1 周砚内容**（已批准，docs/10 §1.2）：版本签名站务帖＋第一次文字联络（P05 结论区站内消息）＋EV28《迁移项目组值班说明》＋p1 错误选项反馈引用。
- [ ] **D2 皮肤升级（已按裁剪顺序牺牲）**：时间流排布、引用层级视觉、米灰底未做；已落地的部分＝周砚站务帖（米色调站务卡）、postsLinked 四段式完成面板、EV14 编号退位。剩余转入 Batch 4 首项。

### E. 存档与类型

- [x] **E1** `schemaVersion` 2→3、`contentVersion`→1.2；旧档载入时明确提示"证据系统改版前版本，进度不能直接延续；可另开新局，原文件保留"，不静默丢弃、不伪造迁移。
- [x] **E2** 固定托盘沿用既有 `pinnedEvidence` 字段（schema 3 内合法），一次性规则卡 cue 复用 `seenPresentationCues`；persistence 单测覆盖 v1/v2 拒绝路径与 v3 通过路径。

### F. 测试与验收（自动化替代盲测，不可裁）

- [x] **F1 单测**：固定上限、来源四档映射、状态派生、EV28 注册检查、schemaVersion 3 拒绝旧档（projection/persistence 两个 spec 更新，57 条全绿）。
- [x] **F2 e2e 回归**：`full-paths.spec.ts` 六路线（SELF/REPLAY×A/B/C）全绿；框架与规格中的旧文案断言（"复查已受理""✓ EV12 已取得"等）全部替换为四段式面板 testid。
- [x] **F3 四档截图**：新增 `screenshots-batch3.spec.ts`（长期资产），360/768/1280/1440 对 P05 三栏、证据抽屉、留言板各存一张至 `gui-test-screenshots/b3-*`，并断言固定托盘 3/3 与站务帖可见。
- [x] **F4 前十分钟走查清单**：以 `vertical-slice.spec.ts`（SELF＋REPLAY）为脚本全绿；逐条核对结果见附录。

---

## 3. 验收标准（Batch 3 完成定义）

1. `npm run typecheck`、`npm run test:unit`、`npm run test:e2e` 全绿。
2. P05 任意时刻暂停，十秒内可从页面指出当前目标、缺少条件、主操作（08 册 §4 阶段完成条件）。
3. 玩家界面首屏无裸 `EVxx` 标题、无"谜题一/二"、无禁用证据复选框。
4. 固定托盘上限三份生效且有满员说明。
5. 旧 schemaVersion 2 存档打开时得到明确重开提示。
6. 四段式完成反馈覆盖 B4 所列四处。

## 4. 明确不做（本轮）

- 证据 27→18 剪裁（Batch 4 评审后执行）。
- 林闻旧签认、许棠主动行动、五人选择、三结局重写的内容实装（见 `10_剧情变更提案.md`，逐项待批）。
- "调查记录"→"叙事轨迹"改名事件（属于在线叙述改写章节，Batch 4）。
- 移动端深度适配、性能与可访问性专项（Batch 6）。
- 留言板皮肤若被裁剪，转入 Batch 4 首项。

## 5. 附录一：F4 前十分钟走查记录（自动化替代盲测 · 2026-09-14）

依据：`vertical-slice.spec.ts`（SELF＋REPLAY 两条完整前十分钟路线）全绿；`fallback`/`recovery` 套件全绿；`gui-test-screenshots/b3-*` 四档截图人工核对。

| 检查点 | 结果 | 依据 |
|---|---|---|
| 30 秒内"我是谁、今晚做什么" | 通过 | 新版首页（Batch 1—2 产物）；两条纵切路线均从首页任务句开始 |
| 2 分钟内首个有意义操作 | 通过 | 第一步即"保存原始交接记录"（纵切脚本首命令） |
| 8 分钟内看到第一次异常 | 通过 | 在册 6→5 与预归档通知断言（`p02:prearchive-notice`） |
| 异常后下一步入口明确 | 通过 | 任务栏目标卡指向 P05；截图核对 P05 首屏＝材料箱＋完整问题＋唯一提交按钮 |
| 无命令失败静默 | 通过 | 拒麦、多窗口临时模式、外部更新、草稿恢复、进度恢复套件全部通过 |

**本走查是自动化替代，不等同于人工盲测**：它证明流程可达、反馈存在、失败有出口；"玩家是否真的感到有方向感"仍需按 08 册 §8.2 组织 5 人线下盲测。

## 6. 附录二：截图核对与已知问题（转后续批次）

四档截图产物：`gui-test-screenshots/b3-p05-desk-*`、`b3-evidence-drawer-*`、`b3-forum-*`（360/768/1280/1440）。

| 档位 | 结论 | 备注 |
|---|---|---|
| 1440 / 1280 | 通过 | 三栏＋右侧调查栏无重叠、无遮挡；1280 档信息密度偏高，见问题 1 |
| 768 | 通过 | 调查栏折叠至内容上方，三栏纵向堆叠，无横向溢出 |
| 360 | 通过（有保留） | 单栏堆叠、无横向溢出；调查记录悬浮按钮遮挡材料箱卡片底部一行，见问题 2 |

1. **1280 密度**：主内容三栏＋294px 调查栏并排偏挤。Batch 4 可考虑在 ≤1280 时将结论区并到中栏下方（两栏变纵向两段）。
2. **360 悬浮按钮遮挡**：`NarrativeTrail` 固定右下角入口压住页面底部内容，为全局既有行为，非本轮引入。归 Batch 6 移动端专项（底部留白或抽屉化）。
3. **留言板时间流皮肤**：按裁剪顺序牺牲，转 Batch 4 首项。
4. **EV28 反馈引用条件**：仅在玩家已打开过《迁移项目组值班说明》时出现。盲测时需确认多数玩家会翻完材料箱；否则把引用改为无条件出现并内联该说明摘要。
